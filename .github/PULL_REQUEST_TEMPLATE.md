## What does this change?

## Why?

## How was this tested?

- [ ] `npm run dev` locally (Pages + D1 local emulation)
- [ ] `npx tsc --noEmit` passes
- [ ] Verified against the deployed `/api/loot` endpoints, if relevant

## Checklist

- [ ] No secrets (API keys, real database IDs assumed sensitive) added to tracked files
- [ ] Updated `README.md` / `migrations/` if the API or schema changed
